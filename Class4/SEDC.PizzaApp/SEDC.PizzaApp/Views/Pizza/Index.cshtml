@model List<PizzaViewModel>

<p>@ViewData["Title"]</p>
<p>Pizzas: </p>
@*<ol>
    @for (int i = 0; i < Model.Count; i++)
    {
    <li>@Model.ElementAt(i).Name / @Model.ElementAt(i).Price</li>
    }
</ol>*@
<ol>
    @for (int i = 0; i < Model.Count; i++)
    {
     <li>@Html.Partial("~/Views/Shared/_PizzaDetails.cshtml", Model[i])</li>
    }
</ol>
